<Page
    x:Class="NewCamera.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewCamera"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0">
            <TextBlock x:Name="SharingModeTextBlock" Text="Initializing..." FontSize="18" HorizontalAlignment="Center"/>
        </StackPanel>
        <!-- Add a CaptureElement for the camera preview -->
        <CaptureElement x:Name="PreviewControl" Stretch="UniformToFill" Margin="0,50,0,0" />
        <Button x:Name="PhotoButton" Content="Take Photo" Click="PhotoButton_Click" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20"/>
    </Grid>
</Page>